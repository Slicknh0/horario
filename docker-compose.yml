services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: horario
      POSTGRES_PASSWORD: horario
      POSTGRES_DB: horario
    ports:
      - "5432:5432"
    volumes:
      - horario_pgdata:/var/lib/postgresql/data

volumes:
  horario_pgdata:
